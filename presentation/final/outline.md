# 온디바이스 sLLM용 K26–Memory FPGA 가속기 설계

형식: 16:9 · 17장 · 발표 목표 9:35 · Slide 17 Q&A 시간 제외 · 한국어 기술 컨퍼런스 발표

중심 문장: K26의 계산부와 외부 Memory FPGA의 가중치 공급부를 분리한 확장형 가속기 구조를 설계하고, 실제 Gemma 3 1B 작업에서 작업 불균형을 줄이는 이득이 원격 데이터 이동 비용보다 커지는 조건을 분석하였다.

## 구성

1. 온디바이스 sLLM용 K26–Memory FPGA 가속기 설계 — 0:10 · 실제 Gemma 작업에서 계산·메모리 분리의 채택 조건을 분석했다.
2. 오늘 답할 네 가지 질문 — 0:20 · 문제 재정의에서 구조·검증·채택 조건까지 직선적으로 답한다.
3. 1B 모델은 4GB에 들어갔다—그래서 질문을 바꿨다 — 0:45 · 외부 메모리는 용량보다 공급 분리의 이득이 이동 비용을 넘을 때 의미가 있다.
4. 계산과 가중치 공급을 두 장치로 분리했다 — 0:50 · K26은 연산·제어를, Memory FPGA는 확장 메모리와 가중치 공급을 맡는다.
5. 메모리 용량만 늘리지 않고 작업 배치와 데이터 위치를 함께 설계했다 — 0:35 · 새 스케줄러보다 외부 메모리 가속기가 유효해지는 시스템 조건을 찾는 것이 핵심이다.
6. 실제 Gemma 가중치를 MatVec RTL에서 검증했다 — 0:35 · 세 대표 가중치 타일이 소프트웨어 INT32 기준값과 정확히 일치했다.
7. 정적 배정은 데이터를 가까이 두지만 작업을 고르게 나누지 못한다 — 0:30 · 한 큐에 작업이 몰리면 다른 연산기가 비어 있어도 마지막 작업은 기다린다.
8. 빈 연산기가 이득이 있는 작업만 가져온다 — 0:40 · 기다림 감소 이득과 가중치·활성값 이동 비용을 함께 계산한다.
9. Gemma 그래프를 802개 연산 타일로 나누고 단계 의존 규칙을 부여했다 — 0:35 · 큰 행렬을 출력 방향으로 분할하고 보수적 단계 의존 규칙을 부여했다.
10. 같은 작업에서 정적 배정과 작업 재분배만 비교했다 — 0:45 · 동일 작업·초기값·하드웨어 가정에서 정책만 바꿔 대기 감소와 이동 비용을 분리했다.
11. 작업이 치우친 경우에만 재분배가 효과적이었다 — 0:45 · 초기 배치가 이미 균형이면 이동 비용만 추가될 수 있다.
12. 유휴 연산은 줄었지만 데이터 이동이 새로운 비용이 됐다 — 0:40 · 작업 재분배의 이득은 링크·메모리 비용과 함께 판단해야 한다.
13. 현재 모델 가정에서는 K26 로컬 경로가 유리했다 — 0:50 · 외부 메모리는 용량·대역폭 격리의 이득이 링크 비용을 넘을 때만 채택한다.
14. 핵심 인터페이스를 실제 PCB 객체로 구체화했다 — 0:30 · K26–Memory FPGA 링크와 DDR3L x16 대표 슬라이스를 참조 라우팅으로 검증했다.
15. 다음 단계는 분석 모델을 실제 하드웨어 경로로 닫는 것이다 — 0:30 · K26 기준선부터 물리 경로와 전력·신호 무결성까지 순서대로 검증한다.
16. 결론과 기여 — 0:35 · 구조 제안, 조건 분석, 채택 기준 도출을 하나의 설계 흐름으로 연결했다.
17. 질문과 토론 — 0:00 · 감사합니다.
